default:
  tags:
    - apimhub-shell  # Use your shell runner with native Docker installed

image: docker:latest

stages:
  - build_and_push

variables:
  IMAGE_NAME: "$CI_PROJECT_NAME"
  IMAGE_TAG: "$CI_COMMIT_SHORT_SHA"
  NEXUS_REPO_USER: "svc-apim-cicd-dev"
  NEXUS_REPO_PASS: "F_arX1Z%fIQSfZEM@aJL9RRk*"
  NEXUS_REGISTRY: "fmk.nexus-ci.onefiserv.net/apm/006528"
  FULL_IMAGE_NAME: "$NEXUS_REGISTRY/$IMAGE_NAME:$IMAGE_TAG"

build_and_push_image:
  stage: build_and_push
  script:
    - echo "Logging in to Nexus Docker registry..."
    - echo "$NEXUS_REPO_PASS" | docker login -u "$NEXUS_REPO_USER" --password-stdin $NEXUS_REGISTRY
    - echo "Building Docker image..."
    - docker build -t $FULL_IMAGE_NAME .
    - echo "Pushing Docker image to Nexus..."
    - docker push $FULL_IMAGE_NAME
  rules:
    - changes:
        - "**/*"